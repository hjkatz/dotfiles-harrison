# Instructions

Update Claude Code to the latest version and resume the current session.

This command will:
1. Update Claude Code using npm/npx
2. Automatically resume the current conversation using `claude --resume`
3. Continue right where we left off

Execute the update-claude script from $DOTFILES/claude/bin/update-claude
