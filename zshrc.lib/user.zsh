# user settings for dotfiles

# default user information (for gitconfig, mainly)
# local configs are found in ~/.zshrc_local
export GIT_NAME="Harrison Katz"
export GIT_EMAIL="hjatz03@gmail.com"
export GIT_ORG="hjkatz"
export GIT_GPG_SIGNINGKEY="" # set in ~/.zshrc_local
export GIT_GPG_SIGN="false" # don't sign git actions by default
export GIT_CODE_DIR="$HOME/Code"

# how long in seconds to wait between update checks
UPDATE_CHECK_INTERVAL_SECONDS=86400
